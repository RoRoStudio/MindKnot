import React from "react"; import { View } from "react-native"; import { SafeAreaView } from "react-native-safe-area-context"; import { useNavigation } from "@react-navigation/native"; import { Typography, EntryDetailHeader, Button } from "../../../shared/components"; export default function LoopDetailScreen() { const navigation = useNavigation(); return ( <SafeAreaView style={{ flex: 1 }}> <EntryDetailHeader entryType="Loop Detail" onBackPress={() => navigation.goBack()} /> <View style={{ flex: 1, justifyContent: "center", alignItems: "center", padding: 20 }}> <Typography>Loop Detail Screen</Typography> <Button variant="primary" label="Go Back" onPress={() => navigation.goBack()} /> </View> </SafeAreaView> ); }
